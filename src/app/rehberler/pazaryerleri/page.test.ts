import { describe, expect, it } from 'vitest'
describe('pazaryeri pillar rotası', () => { it('ana mesajı sabittir', () => { expect('/rehberler/pazaryerleri').toBe('/rehberler/pazaryerleri'); expect('kanal katkısını').toContain('katkı') }) })
